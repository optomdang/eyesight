const express = require('express');

const router = express.Router();

module.exports = router;

/**
 * @swagger
 * tags:
 *   name: Schedules
 *   description: RESTful scheduling endpoints (currently disabled)
 */
